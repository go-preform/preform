// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/go-preform/preform/benchmark/ent/predicate"
	"github.com/go-preform/preform/benchmark/ent/testb"
	"github.com/go-preform/preform/benchmark/ent/testc"
)

// TestCQuery is the builder for querying TestC entities.
type TestCQuery struct {
	config
	ctx        *QueryContext
	order      []testc.OrderOption
	inters     []Interceptor
	predicates []predicate.TestC
	withTestB  *TestBQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TestCQuery builder.
func (tc *TestCQuery) Where(ps ...predicate.TestC) *TestCQuery {
	tc.predicates = append(tc.predicates, ps...)
	return tc
}

// Limit the number of records to be returned by this query.
func (tc *TestCQuery) Limit(limit int) *TestCQuery {
	tc.ctx.Limit = &limit
	return tc
}

// Offset to start from.
func (tc *TestCQuery) Offset(offset int) *TestCQuery {
	tc.ctx.Offset = &offset
	return tc
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tc *TestCQuery) Unique(unique bool) *TestCQuery {
	tc.ctx.Unique = &unique
	return tc
}

// Order specifies how the records should be ordered.
func (tc *TestCQuery) Order(o ...testc.OrderOption) *TestCQuery {
	tc.order = append(tc.order, o...)
	return tc
}

// QueryTestB chains the current query on the "test_b" edge.
func (tc *TestCQuery) QueryTestB() *TestBQuery {
	query := (&TestBClient{config: tc.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tc.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tc.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(testc.Table, testc.FieldID, selector),
			sqlgraph.To(testb.Table, testb.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, testc.TestBTable, testc.TestBColumn),
		)
		fromU = sqlgraph.SetNeighbors(tc.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TestC entity from the query.
// Returns a *NotFoundError when no TestC was found.
func (tc *TestCQuery) First(ctx context.Context) (*TestC, error) {
	nodes, err := tc.Limit(1).All(setContextOp(ctx, tc.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{testc.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tc *TestCQuery) FirstX(ctx context.Context) *TestC {
	node, err := tc.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TestC ID from the query.
// Returns a *NotFoundError when no TestC ID was found.
func (tc *TestCQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tc.Limit(1).IDs(setContextOp(ctx, tc.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{testc.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tc *TestCQuery) FirstIDX(ctx context.Context) int {
	id, err := tc.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TestC entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TestC entity is found.
// Returns a *NotFoundError when no TestC entities are found.
func (tc *TestCQuery) Only(ctx context.Context) (*TestC, error) {
	nodes, err := tc.Limit(2).All(setContextOp(ctx, tc.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{testc.Label}
	default:
		return nil, &NotSingularError{testc.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tc *TestCQuery) OnlyX(ctx context.Context) *TestC {
	node, err := tc.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TestC ID in the query.
// Returns a *NotSingularError when more than one TestC ID is found.
// Returns a *NotFoundError when no entities are found.
func (tc *TestCQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tc.Limit(2).IDs(setContextOp(ctx, tc.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{testc.Label}
	default:
		err = &NotSingularError{testc.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tc *TestCQuery) OnlyIDX(ctx context.Context) int {
	id, err := tc.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TestCs.
func (tc *TestCQuery) All(ctx context.Context) ([]*TestC, error) {
	ctx = setContextOp(ctx, tc.ctx, "All")
	if err := tc.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TestC, *TestCQuery]()
	return withInterceptors[[]*TestC](ctx, tc, qr, tc.inters)
}

// AllX is like All, but panics if an error occurs.
func (tc *TestCQuery) AllX(ctx context.Context) []*TestC {
	nodes, err := tc.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TestC IDs.
func (tc *TestCQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tc.ctx.Unique == nil && tc.path != nil {
		tc.Unique(true)
	}
	ctx = setContextOp(ctx, tc.ctx, "IDs")
	if err = tc.Select(testc.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tc *TestCQuery) IDsX(ctx context.Context) []int {
	ids, err := tc.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tc *TestCQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tc.ctx, "Count")
	if err := tc.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tc, querierCount[*TestCQuery](), tc.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tc *TestCQuery) CountX(ctx context.Context) int {
	count, err := tc.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tc *TestCQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tc.ctx, "Exist")
	switch _, err := tc.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tc *TestCQuery) ExistX(ctx context.Context) bool {
	exist, err := tc.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TestCQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tc *TestCQuery) Clone() *TestCQuery {
	if tc == nil {
		return nil
	}
	return &TestCQuery{
		config:     tc.config,
		ctx:        tc.ctx.Clone(),
		order:      append([]testc.OrderOption{}, tc.order...),
		inters:     append([]Interceptor{}, tc.inters...),
		predicates: append([]predicate.TestC{}, tc.predicates...),
		withTestB:  tc.withTestB.Clone(),
		// clone intermediate query.
		sql:  tc.sql.Clone(),
		path: tc.path,
	}
}

// WithTestB tells the query-builder to eager-load the nodes that are connected to
// the "test_b" edge. The optional arguments are used to configure the query builder of the edge.
func (tc *TestCQuery) WithTestB(opts ...func(*TestBQuery)) *TestCQuery {
	query := (&TestBClient{config: tc.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	tc.withTestB = query
	return tc
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BID int `json:"b_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TestC.Query().
//		GroupBy(testc.FieldBID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tc *TestCQuery) GroupBy(field string, fields ...string) *TestCGroupBy {
	tc.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TestCGroupBy{build: tc}
	grbuild.flds = &tc.ctx.Fields
	grbuild.label = testc.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BID int `json:"b_id,omitempty"`
//	}
//
//	client.TestC.Query().
//		Select(testc.FieldBID).
//		Scan(ctx, &v)
func (tc *TestCQuery) Select(fields ...string) *TestCSelect {
	tc.ctx.Fields = append(tc.ctx.Fields, fields...)
	sbuild := &TestCSelect{TestCQuery: tc}
	sbuild.label = testc.Label
	sbuild.flds, sbuild.scan = &tc.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TestCSelect configured with the given aggregations.
func (tc *TestCQuery) Aggregate(fns ...AggregateFunc) *TestCSelect {
	return tc.Select().Aggregate(fns...)
}

func (tc *TestCQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tc.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tc); err != nil {
				return err
			}
		}
	}
	for _, f := range tc.ctx.Fields {
		if !testc.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tc.path != nil {
		prev, err := tc.path(ctx)
		if err != nil {
			return err
		}
		tc.sql = prev
	}
	return nil
}

func (tc *TestCQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TestC, error) {
	var (
		nodes       = []*TestC{}
		_spec       = tc.querySpec()
		loadedTypes = [1]bool{
			tc.withTestB != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TestC).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TestC{config: tc.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tc.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := tc.withTestB; query != nil {
		if err := tc.loadTestB(ctx, query, nodes, nil,
			func(n *TestC, e *TestB) { n.Edges.TestB = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (tc *TestCQuery) loadTestB(ctx context.Context, query *TestBQuery, nodes []*TestC, init func(*TestC), assign func(*TestC, *TestB)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TestC)
	for i := range nodes {
		fk := nodes[i].BID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(testb.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "b_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (tc *TestCQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tc.querySpec()
	_spec.Node.Columns = tc.ctx.Fields
	if len(tc.ctx.Fields) > 0 {
		_spec.Unique = tc.ctx.Unique != nil && *tc.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tc.driver, _spec)
}

func (tc *TestCQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(testc.Table, testc.Columns, sqlgraph.NewFieldSpec(testc.FieldID, field.TypeInt))
	_spec.From = tc.sql
	if unique := tc.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tc.path != nil {
		_spec.Unique = true
	}
	if fields := tc.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, testc.FieldID)
		for i := range fields {
			if fields[i] != testc.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if tc.withTestB != nil {
			_spec.Node.AddColumnOnce(testc.FieldBID)
		}
	}
	if ps := tc.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tc.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tc.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tc.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tc *TestCQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tc.driver.Dialect())
	t1 := builder.Table(testc.Table)
	columns := tc.ctx.Fields
	if len(columns) == 0 {
		columns = testc.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tc.sql != nil {
		selector = tc.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tc.ctx.Unique != nil && *tc.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tc.predicates {
		p(selector)
	}
	for _, p := range tc.order {
		p(selector)
	}
	if offset := tc.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tc.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TestCGroupBy is the group-by builder for TestC entities.
type TestCGroupBy struct {
	selector
	build *TestCQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tcb *TestCGroupBy) Aggregate(fns ...AggregateFunc) *TestCGroupBy {
	tcb.fns = append(tcb.fns, fns...)
	return tcb
}

// Scan applies the selector query and scans the result into the given value.
func (tcb *TestCGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tcb.build.ctx, "GroupBy")
	if err := tcb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TestCQuery, *TestCGroupBy](ctx, tcb.build, tcb, tcb.build.inters, v)
}

func (tcb *TestCGroupBy) sqlScan(ctx context.Context, root *TestCQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tcb.fns))
	for _, fn := range tcb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tcb.flds)+len(tcb.fns))
		for _, f := range *tcb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tcb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tcb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TestCSelect is the builder for selecting fields of TestC entities.
type TestCSelect struct {
	*TestCQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tc *TestCSelect) Aggregate(fns ...AggregateFunc) *TestCSelect {
	tc.fns = append(tc.fns, fns...)
	return tc
}

// Scan applies the selector query and scans the result into the given value.
func (tc *TestCSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tc.ctx, "Select")
	if err := tc.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TestCQuery, *TestCSelect](ctx, tc.TestCQuery, tc, tc.inters, v)
}

func (tc *TestCSelect) sqlScan(ctx context.Context, root *TestCQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tc.fns))
	for _, fn := range tc.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tc.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tc.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
